<p><%= comment.author %> (<em><%= comment.emails %></em>) said:</p>

<blockquote>
  <%= comment.body %><br>
  <small><em><%= friendly_date comment.created_at %></em></small><br />
  <%= link_to 'Destroy', [comment.post, comment], method: :delete, data: { confirm: 'Delete comment?' } %>
</blockquote>